# ThinQ Real 사내 전환 검증(UAT) 체크리스트 — v1 (2026-09-21)

> **담당**: 협업자(UAT 담당 — 실사용 관리자) / **승인·지원**: 담당자(강원석 책임) / **작성**: 외부 트랙
> **목적**: 사내 컨테이너(QA 환경)가 현행 사이트(thinqreal.com)와 **똑같이 동작하는지** 실사용자 눈으로 끝까지 돌려보고, 다른 점을 찾아내는 것. 코드를 볼 필요 없고, 평소 관리자 페이지를 쓰듯 **누르고 → 기대와 같은지 적는** 일이다.
> **끝의 정의**: 아래 표의 모든 항목에 판정(○ 같음 / △ 다르지만 무해 / × 다름·오류)이 채워지고, △·× 항목이 "차이 보고 양식"으로 담당자에게 전달되면 끝.

## 0. 시작 전 준비 (담당자와 함께 1회)

| # | 준비 | 방법 | 확인 |
|---|---|---|---|
| 0-1 | QA 주소 | `https://kic-qa-thinq-real.thinqcloud.link` — **사내망(사내 PC)에서만** 열림. 즐겨찾기 등록 | ☐ |
| 0-2 | SSO 로그인 + **캐시 주의** | 주소를 열면 사내 로그인 화면 → 본인 계정. 관리자 페이지 주소는 `…/thinqreal_admin.html`. **처음 열 때 반드시 Ctrl+F5(강력 새로고침)** — 예전에 열어본 적이 있으면 브라우저가 옛 페이지(구글 백엔드 호출본)를 보여줘서 코드 요청이 엉뚱한 곳으로 간다(9/22 실제 발생). 확실히 하려면 시크릿 창 사용 | ☐ |
| 0-3 | 관리자 인증 코드 | 관리자 페이지에서 이메일 인증 [코드 요청]을 **한 번만** 누른 뒤(QA는 메일이 실제로 오지 않음 — 60초가 지나 다시 누르면 **새 코드가 발급돼 먼저 본 코드는 무효**), **새 탭에서** `https://kic-qa-thinq-real.thinqcloud.link/api?type=auth_code_peek&kind=admin&email=본인메일` 을 열면 `"code":"123456"` 이 보인다 → 그 6자리를 입력. 메인 사이트 게이트(1-1)는 `kind=auth` 로 같은 방법. 토큰은 7일 유효. **"인증 코드가 일치하지 않습니다"가 나오면** 페이지를 거치지 말고 같은 탭 주소창에서 순서대로 ① `…/api?type=admin_auth_request&email=본인메일` → `ok:true`(60초 이내 재요청은 `cooldown`) ② peek 주소를 Ctrl+F5로 3번 열어 `code`가 매번 같은지 ③ `…/api?type=admin_auth_verify&email=본인메일&code=그6자리` → `ok:true`면 정상. 실패하면 JSON의 `error` 값(`code_mismatch`/`code_expired`/`too_many_attempts`)과 peek의 `pod`·`kv` 값을 담당자에게 그대로 전달. **담당자 통과(2026-09-22, 키트 v4.3)** — 주소창 경로·페이지 경로 모두 대시보드 진입 | ☑ |
| 0-0 | **선행 조건(담당자)** | 키트 v4.1(정적 파일 최신화 + API 자동 치환)이 QA에 배포돼 있어야 한다 — 그 전에는 QA 페이지가 **라이브 운영 백엔드를 호출**하므로 UAT 금지. **2026-09-22 QA 준비 완료 확인(담당자)** — 0.13.0, 치환·peek 실측 통과 | ☑ |
| 0-4 | 비교 기준 | 현행 사이트 `https://thinqreal.com` 관리자 페이지를 **옆 창에** 띄워 두고 화면·동작을 나란히 비교 | ☐ |
| 0-5 | 테스트 데이터 규칙 | 입력하는 이름·주제 앞에 **`[UAT]`** 를 붙인다 (예: `[UAT] 홍길동`). 검증이 끝나면 `[UAT]` 데이터는 전부 삭제 — QA 데이터는 남는다 | ☐ |
| 0-6 | 기록 | 이 문서를 인쇄하거나 복사해 판정·비고를 채운다. 화면 캡처는 사내 정책상 불가 → **문구를 그대로 옮겨 적기** | ☐ |
| 0-7 | (선택) 사내 Claude | 막히면 사내 Claude Code 새 채팅에 "브리핑과 UAT 체크리스트 기준으로 N-N 항목이 막혔어: (문구)" 라고 물으면 안내한다 | ☐ |

**QA에서는 안 되는 것(정상)**: 메일 실발송·텔레그램·캘린더 등록 — 전부 "억제"돼 있어서 화면상 완료돼도 실제 도착하지 않는다. 이것은 9단계(OP·SMTP 이후)에서 본다.

## 1. 예약 흐름 — 메인 사이트 (`/`)

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 1-1 | `/` 접속 → 이메일 게이트에 본인 @lge.com 입력 → 코드 요청 | "코드를 보냈다" 안내(실제 메일은 안 옴 — 담당자가 코드 전달) → 코드 입력 → 홈 화면 | | |
| 1-2 | 홈 카드 순서 | 쇼룸 지원 → 기술 연구·검증 → 데이터 축적 순, ABOUT 영상 재생 | | |
| 1-3 | 공간 소개 | 01 거실 → 06 현관·복도 순서·사진 6장 | | |
| 1-4 | 예약 달력 — **오늘·내일** 클릭 | 선택 불가(회색), 달력 아래 "2일 이내는 담당자 유선 문의" 안내 | | |
| 1-5 | 예약 달력 — **모레 이후** 날짜 클릭 | 슬롯 3개(09:30–11:00 / 13:30–15:00 / 15:30–17:00) 표시 | | |
| 1-6 | 슬롯 다중 선택 | 두 개 이상 선택 가능, 선택 표시 유지 | | |
| 1-7 | 폼 — 신청자 그룹(이름·직급·이메일) 입력 → 「방문 책임자와 신청자가 동일합니까?」 **미체크** | 책임자 이름·직급 칸이 보임 | | |
| 1-8 | 같은 질문 **체크** | 책임자 칸이 숨고 신청자 값이 복사됨 | | |
| 1-9 | 소속 본부 드롭다운 | 현행 사이트와 같은 본부 목록 | | |
| 1-10 | 방문 목적 6종 각각 선택 | 목적마다 동적 영역(주제/명단/활용/기대)이 현행과 같이 바뀜 | | |
| 1-11 | 방문자 명단 11명째 추가 시도 | 10명에서 막힘(안내 문구) | | |
| 1-12 | 활용 방안·기대 효과에 29자 입력 후 제출 | "30자 이상" 안내로 제출 차단 | | |
| 1-13 | 필수 동의 2종(수집·이용 / 파손·분실) 중 하나 미체크 후 제출 | 제출 차단 + 안내 | | |
| 1-14 | 모두 채우고 제출 (이름 `[UAT] 본인이름`) | 완료 안내. **관리자 페이지 예약 관리에 "대기중"으로 나타남** (2단계에서 확인) | | |
| 1-15 | 이용 안내 페이지 | 유의사항 5그룹, 주차 약도, 웰컴 보드, 담당자 표기가 현행과 동일 | | |

## 2. 관리자 — 📋 예약 관리

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 2-1 | 관리자 페이지 로그인(0-3 코드) | 대시보드 진입, 사이드바 탭 목록이 현행과 동일 | | |
| 2-2 | 1-14의 예약이 목록에 있는지 | "대기중", 신청자·책임자·본부·목적·회차가 입력대로 | | |
| 2-3 | 상세 모달 열기 | 명단·활용·기대 등 입력 내용 전부 표시 | | |
| 2-4 | [승인] | 상태 "확정"으로 변경 (메일·캘린더는 QA 억제 — 화면만) | | |
| 2-5 | 같은 날짜·회차로 예약을 하나 더 넣고(1단계 반복) [거절] | 상태 "거절" | | |
| 2-6 | 이력 추가(관리자 직접 입력) — **오늘 날짜**로 | 등록됨 (관리자 입력은 D+2 제한 없음), 알림 없음 | | |
| 2-7 | 상세 수정 — 부서·주제 바꾸기 | 저장 후 반영, id·접수시각은 그대로 | | |
| 2-8 | 상세 모달 [🖨 동의서] | 방문일·회사·안내 담당자(=책임자)·명단이 채워진 서면 동의서 창 | | |
| 2-9 | CSV 내보내기 — 사유 4자 / 비밀번호 7자 | 둘 다 거부(5자+/ASCII 8자+) | | |
| 2-10 | CSV 내보내기 — 정상 입력 | 암호 ZIP 다운로드, 비밀번호로 열림 | | |
| 2-11 | 영구 삭제 — "삭제" 대신 다른 글자 입력 | 삭제 안 됨 | | |
| 2-12 | 영구 삭제 — "삭제" 정확 입력 | 목록에서 사라짐 | | |
| 2-13 | 새로고침 후 목록 | 삭제된 건 없고 나머지는 그대로 (데이터 유지) | | |

## 3. 관리자 — 🚫 슬롯 제어

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 3-1 | 임의 날짜 1회차 차단(사유 `[UAT]`) | 차단 목록에 등록 | | |
| 3-2 | 메인 사이트 달력에서 그 날짜 | 1회차 "예약 불가"(회색), 2·3회차는 선택 가능 | | |
| 3-3 | **5분 뒤** 관리자 페이지 새로고침 | 차단이 그대로 남아 있음 (데이터 유지) | | |
| 3-4 | 차단 해제 | 메인 달력에서 다시 선택 가능 | | |

## 4. 관리자 — 📊 통계 · 🔐 연동 계정 · 🎬 시연 · 💡 조명 · ⚙️ 구성 · 📦 가전

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 4-1 | 통계 탭 | 방문 목적 차트에 6개 카테고리가 0건 포함 항상 표시, 사업부별 차트 | | |
| 4-2 | 연동 계정·시연 시나리오·조명 스위치·시스템 구성 | 현행과 내용 동일 (욕실 조명 "3구 미사용" 표기 있음) | | |
| 4-3 | 구비 가전 | 45개, 순서가 현행과 동일 | | |

## 5. 설문 · 대장 · 이슈 · 방문자 · 큐레이션

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 5-1 | `/ThinQ_Real_Visit_Survey.html` 열기 → **media 트랙**으로 8번 블록까지 전 문항 응답 (소속 `[UAT]`) | 제출 완료 | | |
| 5-2 | 관리자 📝 설문·대장 탭 → 응답 서브탭 | 5-1 응답 표시, 상세 모달에 전 문항 | | |
| 5-3 | 5-1에서 캠페인 연결·이슈 발견을 "예"로 했을 때 | 대장에 "후보" 행, 이슈에 "등록" 행이 **자동 생성** | | |
| 5-4 | 대장 상태 후보 → 확정(금액 미상) | 전환됨, ROI 반영 체크는 금액 없으면 불가 | | |
| 5-5 | 이슈 속성(장비·증상·심각도) 입력 | 저장·표시 | | |
| 5-6 | sales 트랙으로 설문 열기 | 8번 블록이 **안 보임**(현행과 동일) | | |
| 5-7 | `/ThinQ_Real_Visitor_Survey.html` — **시크릿 창(로그인 없이)** 열기 | 로그인 없이 열림, 한/영 토글 동작 | | |
| 5-8 | 방문자 설문 제출(EN 화면으로) | 관리자 방문자 서브탭에 표시, 저장값은 한국어 | | |
| 5-9 | 방문자 응답 영구 삭제("삭제" 타이핑) | 사라짐 | | |
| 5-10 | 베스트 리뷰어 서브탭 | 월 현황·이력·당월 후보 화면 표시 (발송은 QA 억제 — 누르지 않아도 됨) | | |
| 5-11 | 리포트 준비 — 인사이트 1건·한마디 1건 추가, ▲▼ 순서 이동 | 추가·이동 반영 | | |
| 5-12 | 기사 URL 추가(아무 뉴스 링크) → [수정] 모달로 제목 교정 | 제목·썸네일 자동 추출 또는 수정값 반영 | | |
| 5-13 | 설문 원본 내보내기 ⬇ (사유·비밀번호) | CSV+JSONL ZIP 다운로드 | | |
| 5-14 | 5-1 응답 영구 삭제 | 응답과 **파생 대장·이슈 행이 함께** 사라짐 | | |

## 6. 월간 리포트 · ROI

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 6-1 | 리포트 준비 → [리포트 미리보기] (이번 달) | 상단 "미리보기" 배너, KPI 3카드, 사업부별, 목적 도넛(그림), 인사이트·한마디, 기사 카드, 맨 아래 ROI | | |
| 6-2 | 수동 발송 화면 열기 | 수신자 목록·건너뛰기 체크박스·[전체 발송]/[테스트] 버튼 (누르지 않기 — QA 억제라 도착 안 함) | | |
| 6-3 | 📈 ROI 분석 탭 | ROI 툴이 안에 뜸, 별창 열기 동작 | | |
| 6-4 | ROI 시나리오 저장 → 📌 리포트 반영 지정 → 6-1 미리보기 다시 | ROI 섹션 수치가 지정 시나리오로 바뀜 → 지정 해제하면 고정 수치로 복귀 | | |

## 7. 공개 경로 (시크릿 창 — 로그인 없이)

| # | 주소 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 7-1 | `/healthz` | `{"ok":true,"backend":"postgres"}` | | |
| 7-2 | `/privacy.html` | 처리방침 V3.0 표시 | | |
| 7-3 | `/pub` | `{"error":"not_found"}` (정상) | | |
| 7-4 | `/` | **로그인 화면으로 이동해야 정상** | | |

## 8. 정리

| # | 할 일 | 판정 |
|---|---|---|
| 8-1 | `[UAT]` 예약·설문·차단·큐레이션 데이터 전부 삭제 | |
| 8-2 | △·× 항목을 아래 양식으로 담당자에게 전달 | |

## 9. OP·SMTP 연결 후 (담당자가 시점 알려줌 — 그 전엔 건너뜀)

| # | 할 일 | 기대 결과 | 판정 | 비고 |
|---|---|---|---|---|
| 9-1 | 예약 접수 | 담당자 3인 + CC에 알림 메일, 텔레그램 도착 | | |
| 9-2 | 승인 | 신청자에게 확정 메일 — 📅일정/📍위치/📶Wi-Fi/🔐도어락/🅿주차/☎문의/📖안내 7항목, R&D면 가전 표·B2B/홍보면 웰컴 보드 첨부 | | |
| 9-3 | 캘린더 | 팀 캘린더에 회차별 일정, 방문자 명단·연락처 미표기 | | |
| 9-4 | 거절 | 거절 메일 | | |
| 9-5 | 방문일 다음날 08:30 | 설문 초대 메일(작성자 = 신청자) | | |
| 9-6 | 리포트 [나에게만 테스트] | [테스트] 접두 리포트 메일 수신, 도넛 그림·기사 썸네일 표시 | | |

## 차이 보고 양식 (△·× 항목마다 한 건)

```
[UAT] 항목 번호: (예: 2-8)
화면: (예: 관리자 > 예약 관리 > 상세 모달)
한 일: (누른 것·입력한 값)
기대: (체크리스트의 기대 결과)
실제: (화면에 뜬 문구를 그대로)
현행 사이트에서는: (같은 조작의 결과 — 비교했으면)
```

## 주간 계획 (권장)

| 주 | 범위 | 끝의 정의 |
|---|---|---|
| 1주차 | 0·1·2단계 | 1·2단계 판정 전부 기입 |
| 2주차 | 3·4·5단계 | 판정 전부 기입 |
| 3주차 | 6·7·8단계 + 차이 보고 | 보고서 전달 |
| SMTP 연결 후 | 9단계 | 판정 기입 |

매주 담당자와 15분 — "이번 주 표에서 어디까지 채웠나 / 막힌 항목" 두 가지만.
